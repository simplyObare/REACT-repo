

const User = () => {

const user = {
   name: 'solx',
   email: 'solx@mail.com'
}

  return (
    <div>
      <p>Name: {user.name}</p>
      <p>email: {user.email}</p>
    </div>
  )
}

export default User
